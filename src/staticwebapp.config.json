{
    "navigationFallback": {
        "rewrite": "/index.html"
        "exclude": ["*.{jpg,gif,png,css}"]
    },
    "routes": [
    {
        "route": "/.well-known",
        "rewrite": "/",
        "statusCode": 301
    }
]
}